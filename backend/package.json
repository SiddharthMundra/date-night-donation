{
  "name": "date-night-fund-api",
  "version": "1.0.0",
  "description": "Lambda backend for Date Night Fund",
  "main": "index.js",
  "scripts": {
    "zip": "npm install && zip -r lambda.zip index.js node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  }
}
